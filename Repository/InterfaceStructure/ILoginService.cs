using News_Portal.Models;
using News_Portal.ModelsDto;
using News_Portal.ModelsDtos;

namespace News_Portal.Repository.InterfaceStructure
{
    public interface ILoginService
    {
        //Task<ResponseDtos> Register(UserDtos userDto);
        //Task<ResponseDtos> Login(LoginDtos loginDto);
        //Task<string> ForgotPassword(ForgotPasswordDto forgotPasswordDto);
        //Task<string> ResetPassword(ResetPasswordDto resetPasswordDto);
    }
}
